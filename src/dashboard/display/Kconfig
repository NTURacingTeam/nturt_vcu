config LV_COLOR_16_SWAP
	default y if LV_COLOR_DEPTH_16

config VCU_DISPLAY
	bool
	depends on VCU_DASHBOARD
	default y
	select DISPLAY
	select LVGL

config VCU_DISPLAY_SHELL
	bool
	depends on VCU_DASHBOARD_SHELL
	default y
	select LV_Z_SHELL
